{{!-- Página principal - homepage --}}
<div class="bg-light p-5 rounded-lg m-3">
  <h1 class="display-4">Bem vindo ao Blog do Node!</h1>
  <p class="lead">Esse é um blog construido utilizando NodeJS + Express + MongoDB.</p>
  <hr class="my-4">
  <p>It uses utility classes for typography and spacing to space content out within the larger container.</p>
  <a class="btn btn-primary btn-lg" href="#" role="button">Crie uma conta</a>
</div>

<hr>

<h4>Postagens recentes</h4>

{{#each postagens}}
<div class="card mt-4">
  <div class="card-body">
    <h3>{{titulo}}</h3>
    <p>{{descricao}}</p>
    <a href="/postagem/{{slug}}"><button class="btn btn-secondary">Leia mais</button></a>
    <hr>
    <small>{{categoria.nome}}</small> - 
    <small>{{#formatDate data}}{{/formatDate}}</small>
  </div>
</div>
{{else}}
<h4 class="form-control mt-4">Nenhuma postagem encontrada.</h4>
{{/each}}